services:
  - type: web
    name: prompt-firewall-backend
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm --workspace backend start
    envVars:
      - key: NODE_ENV
        value: production
